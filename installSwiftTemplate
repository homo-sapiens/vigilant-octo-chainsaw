# The path of the install directory

installDirectory=~/Library/Developer/Xcode/Templates/Custom
echo "Templates will be installed to ${installDirectory}"

# The name of the project template
templateName="PunicApp Swift App"

mkdir -p "${installDirectory}"  
cp -r "${templateName}.xctemplate" "${installDirectory}"

# Create empty directories in the base template folder for common directory str$
declare -a emptyDirectories=(
    "App"
    "ViewControllers/BaseViewControllers"
    "Views/BaseViews"
    "Views/TableViewCells"
    "Views/CollectionViewCells"
    "Views/CustomViews"
    "Analytics"
    "Controllers/BaseControllers"
    "Models"
    "ResourcesFolder"
    "Storyboards"
    "ThirdParty"
    "Utilities/Extensions"
    "Utilities/Managers"
    )

for directory in "${emptyDirectories[@]}"; do
    mkdir -p "${installDirectory}/${templateName}.xctemplate/${directory}"
done
